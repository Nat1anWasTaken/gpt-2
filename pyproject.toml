[project]
name = "gpt-2"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "datasets>=5.0.1",
    "huggingface-hub>=1.32.0",
    "numpy>=2.5.3",
    "safetensors>=0.8.0",
    "tokenizers>=0.23.2",
    "torch>=2.14.0",
]
